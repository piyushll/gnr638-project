# Minimum-version floors. Installed by setup.bash inside the gnr_project_env
# conda env (python 3.11) on the L40s grading host (Linux, CUDA 12.6, 48 GB
# VRAM). The pip torch wheels ship CUDA 12.4 runtime libs, which load fine on
# a 12.6 driver.
torch>=2.5
torchvision>=0.20

# Pinned exactly to 4.49.0: transformers >=4.55 switched the AWQ backend
# from autoawq to gptqmodel, breaking the AWQ load path that this submission
# ships. 4.49.0 is the version another verified-working Project 2 submission
# used, so it is empirically known to load Qwen2.5-VL-32B-Instruct-AWQ
# correctly via autoawq.
transformers==4.49.0
accelerate>=1.3
safetensors>=0.5
huggingface-hub>=0.28
tokenizers>=0.21

# Image preprocessing helper from the Qwen team (smart_resize etc.). Optional
# at inference — the native processor handles PIL images directly.
qwen-vl-utils>=0.0.10

# IO / data.
pillow>=10
pandas>=2
numpy>=1.26,<3
